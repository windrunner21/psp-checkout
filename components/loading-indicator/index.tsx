import styles from "../loading-indicator/LoadingIndicator.module.css"

const LoadingIndicator = () => {
    return (
        <span className={styles.loader}></span>
    )
}

export default LoadingIndicator